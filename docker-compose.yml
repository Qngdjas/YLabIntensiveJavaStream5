services:
  postgres:
    image: postgres:latest
    container_name: habits-tracker-postgres
    environment:
      POSTGRES_DB: habits_tracker
      POSTGRES_USER: habits_watcher
      POSTGRES_PASSWORD: habits_safety

    ports:
      - "5433:5432"
